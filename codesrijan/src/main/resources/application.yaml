spring:
  application:
    name: codesrijan
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DB_USER:postgres.bsefurbhsfcbwgtvtxtw}
    password: ${DB_PASSWORD:Rajbir112233445}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: ${PORT:8080}
